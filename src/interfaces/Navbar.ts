export interface Props {
    sectionName: string;
    dropdown?: boolean;
    integration?: boolean;
    connector?: boolean;
    authentication?: boolean;
    authenticationLink?: boolean;
    integrationsLink?: boolean;
}